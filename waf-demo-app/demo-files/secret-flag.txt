FLAG{demo_traversal_successful}

*** PLANTED FAKE SECRET — FOR WAF DEMO PURPOSES ONLY ***

This file exists to demonstrate a successful path traversal attack.
It is NOT a real secret. The WAF (Phase 5) will block the request before it ever reaches this file.

Reached via: /api/file?name=subdir/../secret-flag.txt
